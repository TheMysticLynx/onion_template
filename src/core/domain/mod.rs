/// Most atomized domain logic
pub mod entities;
/// Wrappers around implementations of repositories(for example, a database, or file system)
pub mod repositories;
/// Wrappers around implementations of services(for example, a web service, or a database)
pub mod services;